# Generated documentation artifacts — rebuild from docs/*.md, do not commit
docs/*.pdf
docs/*.html

# Go build output
/bin/
/dist/
*.exe
*.test
*.out

# Local environment
.env
.env.local

# Claude Code session state — worktrees under .claude/worktrees/ are whole
# checkouts, and the rest is per-machine session state. Never committed.
/.claude/

# macOS
.DS_Store
